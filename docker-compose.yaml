services:
  05-nest-clean-pg:
    container_name: 05-nest-clean-pg
    image: bitnami/postgresql:17.2.0
    ports:
      - 5432:5432
    environment:
      POSTGRESQL_USERNAME: ${DATABASE_USERNAME}
      POSTGRESQL_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRESQL_DATABASE: ${DATABASE_NAME}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
      interval: 5s
      timeout: 2s
      retries: 20

    networks:
      - 05_nest_clean_network

    volumes:
      - 05_nest_clean_data:/bitnami/postgresql

  05-nest-clean-api:
    build: .
    ports:
      - 3333:3333
    depends_on:
      05-nest-clean-pg:
        condition: service_healthy
    env_file:
      - .env

    networks:
      - 05_nest_clean_network
networks:
  05_nest_clean_network:
    driver: bridge

volumes:
  05_nest_clean_data:
#TODO: use this https://github.com/viniciusferreira7/finance-api/blob/main/compose.yml
