name: CI

on:
  push:
    branches: ["main", "chore/pipeline"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    name: "Build and push"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
